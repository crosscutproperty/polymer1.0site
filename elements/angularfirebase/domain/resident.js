var Resident = function(name, dob) {
  this.name = name;
  this.dob = dob;
};
var billTo = function(name, addr, city, state, zip){
  this.name = name;
  this.addr = addr;
  this.city = city;
  this.zip = zip;
}

